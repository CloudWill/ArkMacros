extends layout

block content
  h1= title
  
  ul
  each server in server_list
    li 
      a(href=server.url) #{server.server_name} 
      | (#{server.server_id})

  else
    li There are no servers.
